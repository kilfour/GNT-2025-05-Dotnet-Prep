# Refactor-safe: Default Groet

Deze functie gebruikt een standaardwaarde in de parameterlijst.

Kun jij dit herschrijven met `if`-logica in plaats van een default parameter — zonder dat de tests falen?
